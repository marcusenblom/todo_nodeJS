<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>All comments</title>
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>

    <div class="header">

    </div>

    <div class="listBody">
        <div class="commentContainer">
            <% allTodos.forEach( (todo)=> { %>
            <div class="commentContainerInner">
                <div class="div1_inner2"><p><strong><%= todo.title %></p></strong></div>
                <div class="deleteButton"><a class="updateButton" href="/update/<%= todo._id %>">update</a><a href="/delete/<%= todo._id %>">x</a></div>
            </div>
        <% }) %>
        </div>
        <div class="formContainer">
            <form action="/todos" method="post">
                <input class="field" type="text" name="title" placeholder="Rubrik">
                <button class="submitButton" type="submit">POST</button>
            </form>
        </div>
    </div>

</body>

</html>